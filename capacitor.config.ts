import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tigriss-mobile-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
